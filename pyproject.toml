[project]
name = "fashion-style-finder-rag"
version = "1.0.0"
description = "IA system to analyze fashion & recommanding clothes"
authors = [
    {name = "julienlucas", email = "hello@julienlucas.com"}
]
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "django>=5.0",
    "django-cors-headers>=4.3.0",
    "langsmith>=0.2.0",
    "mistralai>=1.9.2",
    "numpy<2",
    "pandas>=2.3.1",
    "pillow>=11.3.0",
    "pydantic>=2.11.7",
    "python-dotenv>=1.1.1",
    "requests>=2.28.1",
    # "scikit-learn>=1.7.1",  # Remplacé par numpy (cosine_similarity)
    # "torch==2.2.0",  # Inutile - ONNX Runtime uniquement
    # "torchvision>=0.15.0",  # Inutile - preprocessing PIL/numpy
    "onnxruntime (>=1.23.2,<2.0.0)",
    # "onnx (>=1.20.1,<2.0.0)",  # Inutile - seul onnxruntime est utilisé
    "huggingface-hub (>=0.34.0,<1.0.0)",
    # "onnx2torch>=1.5.15",  # Inutile - script de conversion uniquement
]